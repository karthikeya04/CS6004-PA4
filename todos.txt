* Is object.clone() working as expected?
* can return statement in jimple be like "return $r0.f"? Assumed it's always like "return $r0"
    - In inlineMethod, In for (Unit tu : targetUnits) { loop
        - added a TODO